// Define environment variables
env.DIRECTORY_PATH = '/Users/mariambanda/jenkins/jenkins'
env.TESTING_ENVIRONMENT = 'testing-environment'
env.PRODUCTION_ENVIRONMENT = 'Mariam'

// Define the pipeline stages
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the application...'
               
            }
        }

        stage('Unit and Integration Tests') {
            steps {
                echo 'Running unit and integration tests...'
               
            }
            post {
                
                    success{
                        // Send email notification
                       
                           mail to: 'minasulebanda@gmail.com',
                            subject: "Unit and Integration Tests Stage Result
                            body: "The stage 'Unit and Integration Tests' completed.",
                           
                        
                    }
                
            }
        }

        stage('Code Analysis') {
            steps {
                echo 'Running code analysis...'
               
            }
        }

        stage('Security Scan') {
            steps {
                echo 'Running security scan...'
               
            }
            post {
              
                    success {
                        // Send email notification
                      
                           mail to: 'minasulebanda@gmail.com',
                            subject: "Security Scan Stage",
                            body: "The stage 'Security Scan' completed.",
                            
                        )
                    
                }
            }
        }

        stage('Deploy to Staging') {
            steps {
                echo 'Deploying to Staging...'
                
            }
        }

        stage('Integration Tests on Staging') {
            steps {
                echo 'Running integration tests on staging...'
               
            }
        }

        stage('Deploy to Production') {
            steps {
                echo 'Deploying to Production...'
                
            }
        }
    }
}

